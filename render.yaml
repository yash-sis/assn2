services:
  - type: web
    name: php-app
    env: php
    buildCommand: 'composer install'  # Or other build commands for PHP
    startCommand: 'php -S 0.0.0.0:8080 -t public'  # Adjust if needed
    plan: free
